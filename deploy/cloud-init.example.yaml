#cloud-config
# Example for Ubuntu VPS: run once at first boot (paste into provider "user data").
# Replace git URL / paths with your repo.

package_update: true
packages:
  - ffmpeg
  - ca-certificates
  - git
  - python3
  - python3-pip
  - python3-venv

runcmd:
  # Optional: clone app and create venv (adjust paths)
  # - [ git, clone, "https://github.com/you/AI-Video-Clipper.git", /opt/clipper ]
  # - [ bash, -c, "cd /opt/clipper && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt" ]
